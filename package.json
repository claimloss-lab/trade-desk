{
  "name": "trade-desk-portfolio",
  "version": "1.0.0",
  "description": "SET DR Portfolio Tracker — Cloudflare Pages + Workers",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
